import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'flint-ionic-dashboard-scaffold',
  webDir: 'www'
};

export default config;
